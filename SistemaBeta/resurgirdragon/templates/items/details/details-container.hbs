<fieldset>
    <legend>{{ localize "RESURGIRDRAGON.ItemContainerDetails" }}</legend>

    {{!-- Container Properties --}}
    {{ formField fields.properties options=properties.options label="RESURGIRDRAGON.ItemContainerProperties" localize=true
                 input=inputs.createMultiCheckboxInput stacked=true classes="checkbox-grid checkbox-grid-3" }}

    {{!-- Capacity --}}
    <div class="form-group split-group">
        <label>{{ localize "RESURGIRDRAGON.ItemContainerCapacity" }}</label>
        <div class="form-fields">

            {{!-- Amount --}}
            {{ formField fields.capacity.fields.value value=source.capacity.value label="RESURGIRDRAGON.Amount" localize=true
                         classes="label-top" placeholder="—" }}

            {{!-- Type --}}
            {{ formField fields.capacity.fields.type value=source.capacity.type label="RESURGIRDRAGON.Type" localize=true
                         classes="label-top" choices=config.itemCapacityTypes }}

        </div>
    </div>

    {{!-- Attunement --}}
    {{#if properties.object.mgc}}
    <div class="form-group">
        <label>{{ localize "RESURGIRDRAGON.Attunement" }}</label>
        <div class="form-fields">

            {{!-- Attuned --}}
            {{#if source.attunement}}
            {{ formInput fields.attuned value=source.attuned input=inputs.createCheckboxInput
                         ariaLabel=(localize "RESURGIRDRAGON.Attuned") dataset=(resurgirdragon-object tooltip="RESURGIRDRAGON.Attuned") }}
            {{/if}}

            {{!-- Attunement --}}
            {{ formInput fields.attunement value=source.attunement choices=config.attunementTypes
                         blank="RESURGIRDRAGON.AttunementNone" localize=true }}

        </div>
    </div>
    {{/if}}

</fieldset>
