<div class="tab advancement" data-group="primary" data-tab="advancement">
    <section class="items-list">
        {{#each advancement}}

        <div class="items-section card" data-level="{{ @key }}">

            <div class="items-header header">

                <h3 class="item-name">
                    {{#if (eq @key "0")}}
                    {{ localize "RESURGIRDRAGON.AdvancementLevelAnyHeader" }}
                    {{else if (eq @key "unconfigured")}}
                    {{ localize "RESURGIRDRAGON.AdvancementLevelNoneHeader" }}
                    {{else}}
                    {{ localize "RESURGIRDRAGON.AdvancementLevelHeader" level=@key }}
                    {{/if}}
                </h3>

                <div class="item-header advancement-value">{{ localize "RESURGIRDRAGON.Value" }}</div>

                <div class="item-header item-controls">
                    {{#if (and @root.editable configured (ne @key "unconfigured"))}}
                    <a class="item-control config-button" data-action="modify-choices"
                       data-tooltip="RESURGIRDRAGON.AdvancementModifyChoices"
                       aria-label="{{ localize "RESURGIRDRAGON.AdvancementModifyChoices" }}">
                        <i class="fas fa-cog"></i>
                    </a>
                    {{else}}

                    {{#if (eq configured "full")}}
                    <a class="info-control" data-tooltip="RESURGIRDRAGON.AdvancementConfiguredComplete"
                       aria-label="{{ localize "RESURGIRDRAGON.AdvancementConfiguredComplete" }}">
                        <i class="fas fa-check-circle"></i>
                    </a>
                    {{else if (eq configured "partial")}}
                    <a class="info-control" data-tooltip="RESURGIRDRAGON.AdvancementConfiguredIncomplete"
                       aria-label="{{ localize "RESURGIRDRAGON.AdvancementConfiguredIncomplete" }}">
                        <i class="fas fa-exclamation-triangle"></i>
                    </a>
                    {{/if}}

                    {{/if}}
                </div>

            </div>

            <ol class="item-list unlist">
                {{#each items}}

                <li class="advancement-item item {{ classes }}" data-id="{{ id }}">
                    <div class="item-row">

                        <div class="item-name">
                            <img class="item-image gold-icon" src="{{ icon }}">
                            <div class="name">
                                <div class="title">{{{ title }}}</div>
                                {{#if summary}}
                                <div class="summary">{{{ summary }}}</div>
                                {{/if}}
                            </div>
                            <div class="tags">
                                {{#each tags}}
                                <span data-tooltip="{{ label }}" aria-label="{{ localize label }}">
                                    <resurgirdragon-icon src="{{ icon }}"></resurgirdragon-icon>
                                </span>
                                {{/each}}
                            </div>
                        </div>

                        <div class="item-detail advancement-value {{#unless value}}empty{{/unless}}">
                            {{#if value includeZero=true}}
                            {{ value }}
                            {{/if}}
                        </div>

                        <div class="item-detail item-controls">

                            {{#if @root.editable}}
                            {{!-- Editing --}}
                            <a class="item-control item-action" data-action="edit"
                               data-tooltip="RESURGIRDRAGON.AdvancementControlEdit"
                               aria-label="{{ localize "RESURGIRDRAGON.AdvancementControlEdit" }}">
                                <i class="fas fa-pen-to-square"></i>
                            </a>

                            {{!-- Deleting --}}
                            <a class="item-control item-action" data-action="delete"
                               data-tooltip="RESURGIRDRAGON.AdvancementControlDelete"
                               aria-label="{{ localize "RESURGIRDRAGON.AdvancementControlDelete" }}">
                                <i class="fas fa-trash"></i>
                            </a>
                            {{/if}}

                            {{!-- Context Menu --}}
                            <a class="item-control interface-only" data-context-menu
                               aria-label="{{ localize "RESURGIRDRAGON.AdditionalControls" }}">
                                <i class="fas fa-ellipsis-vertical"></i>
                            </a>

                        </div>

                    </div>
                </li>

                {{/each}}
            </ol>

        </div>

        {{/each}}
    </section>
</div>
